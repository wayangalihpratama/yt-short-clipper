version: '3.8'

services:
  yt-short-clipper:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./output:/app/output
      - ./config.json:/app/config.json
      - ./cookies.txt:/app/cookies.txt
    restart: unless-stopped
